package de.nielsfalk.desktop.filecleaner.domain

import io.kotest.core.spec.style.FreeSpec
import kotlin.test.assertEquals

class FileReaderTest : FreeSpec({
    "foo" {
        assertEquals("foo".length, 4)
    }
})